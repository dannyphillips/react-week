var React = require('react');

var App = React.createClass({
  render () {
    var isOpen = true;
    return (
      <div>
        ohai
      </div>
    );
  }
});

React.render(<App/>, document.getElementById('app'));

