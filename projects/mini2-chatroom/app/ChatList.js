var React = require('react');
var $ = require('jquery');

var ChatList = React.createClass({
  render: function(){
    return (
      <ul className="list-group">
      </ul>
    )
  }
});

module.exports = ChatList;